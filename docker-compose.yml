version: '3'

services:

  python:
    restart: always
    build: .
    command: gunicorn --bind 0.0.0.0:8000 outletavto.wsgi:application
    volumes:
      - ./outletavto:/srv/www/<project>
    expose:
      - 8000
    ports:
      - 8000:8000

  nginx:
    restart: always
    image: nginx:latest
    expose:
      - 8080
    ports:
      - "80:8080"
    depends_on:
      - python
    volumes:
      - ./nginx:/etc/nginx/conf.d
      - ./outletavto/static:/srv/www/outletavto/static